# URL Shortener (Frontend) + Logging Middleware

- Shortens up to 5 URLs at once
- Stores links in Zustand store
- Tracks basic click stats
- Uses reusable logging-middleware package
- Built with React + MUI + Zustand
